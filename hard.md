```
- DOM转矢量pdf
 - 可以试着去接入直接ai对话导入某个页面哪块内容，思路：pupptter加载源代码后，将源码给ai分析出用户的内容是哪块容器，让ai给出domid，然后用我的接口导出。
- trace调用树组件
- 模型选择框
- 基于echarts实现的定制（双axisline）甘特图
- 多目录组件兼容（此博客）
    - 不传容器默认采用锚点形式跳转
    - 传容器采用js控制容器滚动位置（即可以实现一个页面多个容器，多个目录且目录样式可以都不同）
    - 选中子项后，祖先元素展开收起后指示条的位置
- jsonschme
- 提示词输入框友好提示
- 全局返回，兼容各种情况，支持返回带参，无上级的默认跳转，前进后退，步数。考虑接入主应用后的兼容问题。pushreplace
    - 链路1->2->3->4->5，比如3，2->3和4->3和3->3都要支持覆盖参数，增加参数，且每种到达3的行为都不一致。所以要自己去封装维护一个栈，还得兼容浏览器自带的那个返回
- 多页签信息同步
    - 区分不需要同步和需要同步的信息，（通过页面带参决定这两个tab是否同步）
    - 兼容性，降级处理
- 双分页滚动加载兼容
 - 分页滚动加载
 - 游标滚动加载
 - 分页、游标双滚动加载
 - 多列表滚动加载
- html动画转gif
- 多chatai合并的，多结构兼容
    - 结构转换性能优化，
    - 中继转发，考虑到不同接口的数据不同，结构不同，体量不同，导致的提取数据快慢问题，中继负责速度转发，它接收速率无所谓，吐出速率自定义控制。
```